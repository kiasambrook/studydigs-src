/**
 * Launches phpCs and returns results as JSON
 */
interface Options {
    encoding: string;
    standard: string;
}
/**
 * Launches phpCs and returns current version
 */
export declare function version(executablePath?: string): Promise<string>;
interface LinterMessage {
    message: string;
    source: string;
    severity: number;
    fixable: boolean;
    type: 'ERROR' | 'WARNING';
    line: number;
    column: number;
}
interface LintTotals {
    errors: number;
    warnings: number;
    fixable: number;
}
export declare function lint(filenames: string | string[], executablePath?: string, options?: Partial<Options>): Promise<{
    totals: LintTotals;
    files: Record<string, {
        errors: number;
        warnings: number;
        messages: LinterMessage[];
    }>;
}>;
export {};
